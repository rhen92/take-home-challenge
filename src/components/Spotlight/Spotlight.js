import React from 'react';
import './Spotlight.css';

const Spotlight = ({ title, findSelectedArticle }) => {
  const articleWanted = findSelectedArticle(title);
  console.log(articleWanted)
  return (
  
  )
}


export default Spotlight;
